# Higma v1.03-beta 更新日志

**发布日期：** 2026 年 8 月 7 日  
**版本号：** `1.0.3-beta.0`  
**版本性质：** 预览区、编辑记录、保存链路与首次使用界面稳定性更新  
**目标平台：** macOS Apple Silicon（`arm64`）

## 版本概览

本版本重点处理了预览区交互、网页滚动、参数编辑、回退、保存和 Electron 打包过程中的一系列问题。预览区现在使用单一原生 iframe 展示网页，网页自身负责滚动；右侧参数编辑统一进入历史记录；预览网页获得焦点后，快捷键回退也与右上角按钮保持一致。

## 一、预览区重构与网页滚动

### 1. 单 iframe 预览结构

- 重写中间预览区，改为单一原生 iframe。
- 删除此前多层缓存 iframe、手动同步 `scrollTop`、`scrollLeft` 和鼠标滚轮转发逻辑。
- 预览区不再通过外部容器模拟网页滚动。
- iframe 使用原生 `scrolling="auto"`，网页内容高度超过视口时由网页自身处理滚动。

### 2. 鼠标滚轮与触控板

- 移除会拦截滚轮事件的中间层逻辑。
- 鼠标滚轮和触控板事件直接交给网页 iframe。
- 保留原生滚动条拖动能力，同时避免滚轮事件被编辑器外层捕获。
- 在操作网页模式下不显示元素命中框、悬浮框和选中提示，避免干扰网页自身交互。

### 3. 预览区尺寸调整

- 宽度和高度调整条使用 Pointer Capture 处理拖动。
- 拖动结束、指针取消、窗口失焦或丢失指针捕获时都会统一清理状态。
- 调整条默认使用灰色，鼠标悬浮时显示 Higma 蓝色。
- 修复拖动结束后调整条仍跟随鼠标移动的问题。

### 4. 网页显示比例

- 保留预览区右上角全局缩放控件。
- 缩放使用 CSS transform，不改变网页自身布局尺寸。
- 缩放时同步计算预览区外边距，避免覆盖右侧 DESIGN 面板。

## 二、元素选择与编辑行为

### 1. 选择模式和操作模式分离

- “选择元素”模式下才显示悬浮命中框、元素名称和选中边框。
- “操作网页”模式下隐藏所有命中反馈，并恢复网页普通鼠标指针。
- 点击预览区网页外部空白区域可以取消当前选中态。
- 取消选中不会清空已有编辑历史。

### 2. 元素删除

- 右侧选中元素名称旁增加删除按钮。
- 删除动作会进入待保存改动列表，并在保存弹窗中显示“删除元素”。
- 删除保存时由主进程对当前 HTML 源文件执行目标元素删除。
- 删除后保存弹窗可以正常继续保存，不再出现保存按钮无响应。

### 3. SVG 和图片

- 支持粘贴完整 SVG 源码替换选中图标。
- 支持编辑图片地址并写回 `src` 属性。
- SVG 内部颜色和颜色批量替换继续通过统一的颜色编辑流程处理。

## 三、右侧参数编辑

当前可编辑参数包括：

- 字号
- 字重
- 行高
- 字间距
- 左、中、右文字对齐
- 文字颜色
- 宽度与高度
- Padding 四个方向
- Margin 四个方向
- Padding 联动
- Margin 联动
- 容器间隔（`gap`）
- 填充颜色与渐变
- 描边颜色、宽度、开关状态
- 四个独立圆角及圆角联动
- 图片地址
- SVG 源码
- SVG 或容器内颜色组

### 参数拖动稳定性

- 右侧 range 控件使用统一的 Pointer Capture 拖动处理。
- 拖动经过预览区时不会把事件错误转交给预览网页。
- 拖动结束后会释放指针捕获并清理全局调整状态。
- 同一连续拖动操作合并为一个回退步骤，避免产生大量无效历史记录。

## 四、回退、前进与操作历史

### 1. 历史快照

- 历史快照现在同时记录参数值和当时编辑的具体元素。
- 切换选中元素后，之前元素的编辑历史仍然保留。
- 点击预览区空白、切换操作模式、切换普通面板不会清空历史记录。
- 回退时会自动重新定位到对应元素，再恢复其参数状态。
- 对同一参数连续拖动进行批处理；输入框连续修改按时间窗口合并。
- 避免 pointer-down、focus 和 input 重复生成同一条历史记录。

### 2. 快捷键一致性

- 右上角回退按钮和 Cmd/Ctrl+Z 使用同一套回退函数。
- 右上角前进按钮和 Shift+Cmd/Ctrl+Z 使用同一套前进函数。
- 预览 iframe 获得键盘焦点时，会将快捷键转发到编辑器主窗口。
- 支持嵌套预览文档中的快捷键转发。
- 修复部分参数只能通过右上角按钮回退、快捷键无法回退的问题。

### 3. 清理历史的边界

- 用户点击“保存”后，以保存结果作为新的编辑基线。
- 用户在关闭页面时选择“不保存”，会清理该页面的所有内存修改和历史记录。
- 关闭页面时选择“取消”不会改变页面、保存队列或历史记录。
- 普通选择、点击空白和界面操作不会被当成编辑步骤。

## 五、保存与文件安全

### 1. 保存状态

- 保存弹窗列出文件、元素选择器和具体改动类型。
- 保存失败时，错误信息显示在弹窗内部，而不是只写入控制台。
- 支持多个 HTML 文件和同一文件多个元素连续保存。
- 保存过程中按当前磁盘内容继续应用目标修改，避免此前误判为“被其他程序修改”。

### 2. 主进程文件写入

- 保留目标元素定位失败时的明确错误。
- 保留保存请求没有产生实际文件变化时的失败提示。
- 删除元素、文字、样式、图片、SVG 和颜色批量替换继续走统一写入链路。
- 保存完成后同步当前标签页源代码和待保存状态。

## 六、欢迎页调整

- 快速开始卡片移除右上角的“1、2、3”数字标记。
- 底部声明文字改为：

  `Built with Vibe Coding by Jiawei Bu.`

- 底部声明文字颜色适度调亮。
- 底部声明字号从 10px 调整为 12px。

## 七、构建与验证

本版本执行了以下检查：

- TypeScript 项目类型检查通过。
- Electron 主进程 TypeScript 编译通过。
- Vite 生产构建通过。
- 文件保存与预览地址自动化测试通过，共 18 项。
- 覆盖样式、文字、图片、SVG、颜色批量替换、模板元素、多元素连续保存、元素删除和异常保存场景。

## 八、打包信息

- 内部版本号：`1.0.3-beta.0`
- 对外版本标识：`v1.03-beta`
- 打包目标：macOS DMG
- 打包架构：Apple Silicon `arm64`
- 产物目录：`releases/Higma-v1.03-beta`
- 安装包命名：`Higma-v1.03-beta-arm64.dmg`

## 九、已知限制

- 当前构建为 Apple Silicon 测试包，不包含 Intel 或 Universal 安装包。
- 当前应用未配置 Apple Developer ID 签名和公证，首次打开可能需要在 Finder 中右键选择“打开”。
- 在线授权和设备激活功能尚未接入本版本。
