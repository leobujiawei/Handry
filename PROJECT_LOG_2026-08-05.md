# Higma 项目更新日志

**日期：** 2026 年 8 月 5 日  
**对比版本：** 2026 年 8 月 4 日内部版本  
**版本性质：** 编辑体验与保存可靠性重点更新

## 版本概述

本次更新重点完善了 Higma 的首次使用体验、元素选中反馈和右侧排版编辑能力，同时重新梳理了保存机制。用户现在可以更直观地调整间距与圆角，并能明确知道当前正在编辑哪个元素；各种可编辑内容的未保存状态和实际写入也更加可靠。

## 新增功能

### 1. 欢迎页新手引导

- 明确说明 Higma 是用于可视化编辑 AI Coding 生成 HTML 页面的工具。
- 增加“打开 HTML 项目 → 选择元素 → 调整并保存”三步教学。
- 为三个步骤增加文件夹、元素选择和样式调整图标。
- 主按钮统一为“打开文件夹”，并调整到页面中下方。
- 增加底部署名：`Vibe Coding Software by Jiawei Bu`。
- 优化欢迎页元素间距，并适配较窄窗口的纵向布局。

### 2. Padding 与 Margin 联动控制

- Padding 和 Margin 分别增加独立联动开关。
- 开启联动后，左右成对调整、上下成对调整。
- 关闭联动后，上、右、下、左四个方向可以分别调整。
- 四个方向使用不同的方位图标，Margin 图标的方向短线位于方框外侧。

### 3. 圆角独立控制

- 圆角从 Margin 区域中拆出，与 Padding、Margin 保持同一信息层级。
- 增加圆角联动开关。
- 联动模式下统一调整四个圆角。
- 独立模式下可分别调整左上、右上、左下、右下四个圆角。
- 修复圆角方向图标重复显示的问题。

### 4. 当前元素持续选中提示

- 用户点击选中元素后，画布会持续显示粗线高亮框。
- 高亮框左上角显示当前元素名称，便于对应右侧编辑面板。
- 鼠标悬浮其他元素时仍保留原有悬浮提示，不会覆盖当前选中状态。

## 体验优化

- 重新整理右侧“文字”和“排版”区域的标签、控件宽度、间距与对齐关系。
- Padding、Margin 控件与宽度、高度、圆角标签对齐，减少无效留白。
- 联动开关统一放置在控件组右侧。
- 调整 Padding、Margin 输入框宽度，避免控件超出面板或左侧错位。
- 统一数值输入框、单位、方向图标和分隔线的视觉样式。

## 保存可靠性修复

- 修复修改元素后保存按钮仍不可点击的问题。
- 修复保存按钮可点击但改动未真正写入 HTML 文件的问题。
- 保存状态现在覆盖所有可编辑内容：
  - 字号、颜色、宽度、高度
  - Padding、Margin、圆角
  - 文案内容
  - 图片地址
  - SVG 源码及 SVG 内颜色
  - 填充与描边的开启、关闭状态
  - 容器内同色元素的批量颜色替换
- 支持在切换元素、切换文件后继续保留尚未保存的改动。
- 支持同一个文件中多个元素的连续修改与顺序保存。
- 保存时使用最新文件内容继续写入，避免后一次修改覆盖前一次修改。
- 当元素无法定位或保存没有产生真实文件变化时，明确报告失败，不再显示“假保存成功”。
- 保存完成后同步刷新当前文件内容及相关待保存记录。

## 启动与构建

- 更新本地启动脚本，使用 `releases/启动 Visual HTML Editor.command` 启动时会先构建最新前端和 Electron 代码，避免继续加载旧版本界面。

## 验证结果

- TypeScript 类型检查通过。
- Vite 正式版本构建通过。
- Electron 主进程编译通过。
- 保存链路 14 项自动化回归测试全部通过，覆盖样式、文字、图片、SVG、颜色批量替换、多元素连续保存、模板内元素以及异常保存检测。

## 建议重点体验

1. 依次修改不同元素的文字、颜色、间距和圆角，再统一保存并重新打开文件检查结果。
2. 分别验证 Padding、Margin、圆角在联动和独立模式下的行为。
3. 在多个 HTML 文件之间切换后保存，确认每个文件的未保存改动都被保留并写入。
4. 验证元素选中框在鼠标移动到其他元素时仍持续显示。

