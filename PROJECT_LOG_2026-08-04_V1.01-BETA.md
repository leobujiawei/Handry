# Higma 第一版项目日志

**发布日期：** 2026 年 8 月 4 日  
**发布版本：** Higma v1.01-beta  
**内部版本号：** 1.0.1-beta.0  
**版本性质：** 首个可分发 macOS 测试版本

## 版本概述

Higma 第一版完成了从“打开本地 HTML 项目”到“可视化选择元素、调整样式并保存回源文件”的基础闭环。产品定位是帮助用户直接编辑 AI Coding 生成的静态 HTML 页面，让页面调整过程更接近设计工具，同时保留对本地源文件的控制。

## 首版核心流程

1. 打开一个包含静态 HTML 文件的本地文件夹。
2. 从左侧文件列表选择需要编辑的 HTML 页面。
3. 在中间画布中预览页面并点击选择 DOM 元素。
4. 在右侧面板修改文字、颜色、尺寸、间距等属性。
5. 实时查看页面变化，确认后保存回本地 HTML 文件。

## 首版功能

### 1. 本地 HTML 项目管理

- 支持打开本地文件夹并读取其中的 HTML 文件。
- 提供目录树浏览项目文件。
- 支持以标签页方式同时打开多个 HTML 页面。
- 标签页可切换、关闭，并提示文件是否存在未保存改动。

### 2. 可视化页面画布

- 在应用内直接预览本地 HTML 页面。
- 提供“操作网页”和“选择元素”两种模式。
- 选择模式下可通过鼠标悬浮和点击定位页面元素。
- 支持选择嵌套 iframe 和 template 中的元素。
- 支持拖拽调整预览画布宽度与高度。
- 内置桌面、MacBook、iPad、iPhone 等常用设备尺寸。

### 3. 基础样式编辑

- 支持修改字号和文字颜色。
- 支持修改元素宽度与高度。
- 支持分别修改上、右、下、左 Padding。
- 支持分别修改上、右、下、左 Margin。
- 支持统一调整元素圆角。
- 数值属性同时提供滑杆和精确数值输入。
- 修改结果可在画布中实时预览。

### 4. 文字、图片与 SVG 编辑

- 支持直接修改元素中的文字内容。
- 支持修改图片地址或相对路径。
- 支持粘贴完整 SVG 源码替换当前 SVG 图标。
- 支持调整图片和 SVG 的宽度与高度。

### 5. 填充、描边与颜色工具

- 支持开启或关闭元素填充和描边。
- 支持修改纯色填充、文字颜色、描边颜色以及 SVG 颜色。
- 提供带透明度控制的颜色选择器。
- 支持屏幕取色。
- 支持线性渐变编辑，包括渐变角度、颜色节点、节点位置和透明度。
- 可识别选中容器中的颜色，并批量替换使用相同颜色的子元素。

### 6. 批量编辑与操作回退

- 可识别结构和类名相同的同级元素，并支持关联修改。
- 支持编辑过程中的撤销和重做。
- 支持 `Command/Ctrl + Z` 撤销及 `Command/Ctrl + Shift + Z` 重做。

### 7. 保存与历史版本

- 保存前展示待保存文件和变更内容，由用户确认后写入。
- 支持一次确认多个存在改动的 HTML 文件。
- 修改主要通过 inline style 和对应 HTML 属性写回源文件。
- 保存前检查磁盘文件是否仍与载入版本一致，避免覆盖外部程序产生的新修改。
- 使用临时文件和原子替换方式写入，降低文件损坏风险。
- 每次保存前自动生成历史备份。
- 提供“历史版本”入口，可查看并恢复备份文件。

## 首版界面结构

- 顶部：项目操作、HTML 标签页、撤销、重做、历史版本和保存。
- 左侧：项目文件浏览器。
- 中间：网页预览画布、编辑模式和设备尺寸控制。
- 右侧：当前元素信息及样式、文字、媒体、颜色编辑面板。
- 底部：当前操作状态和选择模式提示。

## macOS 发布内容

- 提供 Apple 芯片专用的 `arm64` DMG。
- 提供同时兼容 Apple 芯片和 Intel Mac 的 Universal DMG。
- 提供不包含依赖和构建产物的源码存档。
- 已写入应用名称、图标、Bundle ID 和版本信息。
- 发布包附带 SHA-256 校验文件。

## 首版验证结果

- 生产构建通过。
- TypeScript 类型检查通过。
- Universal 主程序同时包含 `arm64` 和 `x86_64` 架构。
- DMG 映像完整性检查通过。

## 已知限制

- 第一版主要面向静态 HTML，不映射回 React、Vue 或 Svelte 源码。
- 样式修改主要写入 inline style，不直接修改外部 CSS 规则。
- 由脚本动态生成、浏览器自动补全或结构非常复杂的 DOM，可能无法安全映射回源文件；遇到此情况时会拒绝保存。
- 撤销和重做主要针对尚未保存的编辑内容。
- 测试版尚未使用 Apple Developer ID 签名和公证，首次打开可能需要在 Finder 中右键选择“打开”，或前往“系统设置 → 隐私与安全性”确认。

## 发布文件

- `Higma-v1.01-beta-universal.dmg`
- `Higma-v1.01-beta-arm64.dmg`
- `Higma-v1.01-beta-source.zip`
- `README-v1.01-beta.md`
- `SHA256SUMS.txt`

